# 设计文档优化建议

> **来源**: 基于 MathModelAgent、math-modeling-skill、Mamo、AgentLaboratory 四个参考项目的代码级审查
> **日期**: 2026-05-14
> **状态**: 待讨论

---

## 总体判断

设计文档方向正确，基础可行。MathModelAgent 是最合适的二次开发基础——它已有可运行的 4-Agent 流水线、Jupyter/E2B 双后端代码沙箱、错误自纠循环、WebSocket 实时通信、Redis 状态管理、litellm 多模型支持、RAG 知识库等完整基础设施。

以下 10 条优化建议按重要程度排序。

---

## 1. Agent 数量：建议先缩减到 5 个

**现状**: 文档提出 6 Agent（Parser → EDA → Modeler → Coder → Validator → Writer）

**问题**: MathModelAgent 原版是 4 Agent（Coordinator → Modeler → Coder → Writer）。每增加一个 Agent 边界就多一轮 LLM 调用 + 上下文传递损耗。6 Agent 意味着至少 6 轮 LLM 调用，对于复杂题目延迟和 token 消耗显著增加。

**建议**:
- **EDA Agent** → 合并为 ModelerAgent 的一个子阶段。MathModelAgent 的 `MODELER_PROMPT` 已经包含 EDA 规划能力（数据画像、特征工程、可视化建议），只需增强即可
- **Validator Agent** → 合并为 CoderAgent 的反思环节。MathModelAgent 已有 `get_reflection_prompt()` 和 `get_completion_check_prompt()` 用于结果验证

最终结构为 **5 Agent**：Parser → Modeler(含EDA) → Coder(含验证) → Reviewer(新增) → Writer

如果后续测试证明独立 EDA Agent 确实带来显著质量提升，再拆分。

---

## 2. 遗漏了 MathModelAgent 的 HIL（Human-in-Loop）机制

**现状**: 设计文档追求"全程无人干预"，未提及人工审核节点

**问题**: MathModelAgent 已有完整的 checkpoint 系统，支持在关键节点暂停等待用户确认：
- `problem_split` — 题目分解后确认
- `model_selection` — 建模方案后确认
- `code_review_{key}` — 代码执行后审核

对于竞赛场景，建模方向的选择是最需要人工把关的环节。LLM 可能在 EDA 后选择不适合数据的建模方法，人工审核可以及早发现。

**建议**:
- 保留 HIL 机制，至少在 **Stage 3（建模方案设计）** 后加入人工确认节点
- 配置 `HIL_ENABLED=true`，`HIL_TIMEOUT` 设合理值（如 300 秒）
- 其他 checkpoint 可以默认关闭（`HIL_CHECKPOINTS` 按需启用）

---

## 3. EDA Agent 的代码执行能力需要明确定义

**现状**: 文档描述 EDA Agent 要做"自动化数据画像（代码执行）"，但没有说明代码执行机制

**问题**: 如果 EDA Agent 需要写代码并执行，它实际上就是半个 CoderAgent，会引入代码执行沙箱的重复依赖。

**建议**:
- EDA 的数据画像分为两步：
  1. **静态分析**（不执行代码）：pandas 读取数据 → 列类型、缺失率、基本统计量 → 生成 `data_profile.json`
  2. **可视化分析**（需执行代码）：由 CoderAgent 代为执行，EDA Agent 只负责分析结果
- 或者：让 EDA Agent 直接复用 CoderAgent 的 `execute_code` 工具（MathModelAgent 的 tool_registry 支持）

---

## 4. 未充分利用 math-modeling-skill 的 61 算法库

**现状**: 设计文档说"不预设固定算法库，让 LLM 自由选择"

**问题**: math-modeling-skill 的 `assets/` 目录包含 33,000 行算法参考文档，涵盖 61 种算法的完整 Python 实现和可视化代码。纯靠 LLM 记忆选择算法，质量远不如参考专业算法文档。

**建议**:
- 将 math-modeling-skill 的 `assets/` 全部接入 MathModelAgent 已有的 **ChromaDB RAG 知识库**
- MathModelAgent 的 `scripts/build_knowledge_base.py` 已经支持离线索引构建，只需将算法文档放入索引目录
- ModelerAgent 的 Prompt 中已有 `RAG_TOP_K` 配置，可通过知识库检索增强建模决策
- 让 LLM **参考但不限于**这些算法做决策，比纯靠记忆选算法质量高得多

具体做法：
```bash
# 将算法文档复制到 RAG 源目录
cp -r math-modeling-skill/assets/*.md backend/scripts/knowledge_sources/
# 重新构建索引
python backend/scripts/build_knowledge_base.py
```

---

## 5. 长上下文问题缺少具体解决方案

**现状**: 文档将"长上下文信息丢失"列为中等风险，但只写了"分阶段执行 + 结构化中间产物 + 摘要传递"

**问题**: 到 Stage 6（WriterAgent）时，需要汇总前 5 个阶段的所有产物。一个复杂题目可能产生 50K+ tokens 的中间文档。MathModelAgent 的 `max_memory=12`（保留最近 12 条消息）在长流水线中会丢失早期阶段的关键信息。

**建议**:
- 利用 MathModelAgent 的内存压缩机制：当消息数超过 `max_memory` 时，自动将旧消息摘要化
- 在 Stage 间传递**结构化摘要**而非完整文档。例如 WriterAgent 不需要完整的 `notebook.ipynb`，只需：
  - `results.json`（结构化结果数据）
  - 关键图表路径
  - EDA 关键发现（3-5 条要点）
  - 建模决策摘要
- 每个 Agent 在设计 Prompt 时明确"你需要从前序阶段获取什么信息"和"你需要为后续阶段传递什么信息"

---

## 6. 论文生成方案可以更强

**现状**: 使用 `python-docx` 生成 Word 文档

**问题**: `python-docx` 对复杂格式（三线表、公式嵌入、精确排版）支持有限。math-modeling-skill 使用的是 **docx-js**（JavaScript），文档质量更高。

**建议**:
- **MVP 阶段**: 保留 python-docx + pypandoc（MathModelAgent 已实现），快速跑通
- **优化阶段**: 评估是否引入 math-modeling-skill 的 `tools/docx/` 子技能
  - docx-js 支持追踪修订、精确表格控制
  - 或者保持 python-docx 但增强模板配置（MathModelAgent 已有 `md_template.toml`）

---

## 7. 建议增加自评估/质量门禁机制

**现状**: 没有任何自动化质量评估

**问题**: 全自动系统输出论文后，没有机制判断论文质量是否达标。AgentLaboratory 有 3-reviewer 自动评审系统，Mamo 有数值答案验证框架。

**建议**: 在 Stage 6（论文生成）后增加一个可选的 **Reviewer Agent**：
- 用便宜模型（如 DeepSeek-V3 或 GPT-4o-mini）做自动评审
- 评审维度：数据支撑是否充分、建模逻辑是否自洽、图表是否规范、去 AI 味是否到位
- 评审结果作为"修改建议"附在输出中，供用户参考
- 可以参考 AgentLaboratory 的 `ReviewersAgent` 实现（3 种 reviewer 角色：harsh/impact-focused/novelty-focused）

---

## 8. 子问题依赖管理建议先简化

**现状**: 文档提到"DAG 依赖图 + 拓扑排序执行"

**问题**: MathModelAgent 目前的 CoderAgent 是**顺序执行**各子问题的（ques1, ques2, ..., sensitivity_analysis）。数学建模竞赛题大多数子问题本身就有顺序依赖（问题 2 基于问题 1 的结果）。

**建议**:
- **Phase 1**: 保持顺序执行，不引入 DAG。依赖关系通过 CoderAgent 的代码逻辑自然处理
- **Phase 2**: 如果遇到子问题可并行的题目，再考虑拓扑排序

---

## 9. 实施时间线需要调整

**现状**: Phase 1 计划 2 周完成基础框架搭建

**问题**: MathModelAgent 的代码量约 15,000+ 行，架构复杂（Agent 通信、Redis pub/sub、WebSocket 协议、checkpoint 系统、多 LLM 后端、feedback rerun）。代码中大量 `# TODO` 标记，测试覆盖率极低（仅 2 个测试文件）。作者声明"project is in experimental/iterative demo stage"。

**建议**:
| 阶段 | 原计划 | 建议调整 | 理由 |
|------|--------|---------|------|
| Phase 1: 基础框架 | 2 周 | **3-4 周** | 需要深入理解 MathModelAgent 代码、写集成测试、跑通端到端 |
| Phase 2: 数据分析核心 | 2 周 | 3 周 | EDA + 建模需要大量 Prompt 调优 |
| Phase 3: 论文生成 | 2 周 | 2 周 | 可以复用 MathModelAgent 的 Writer |
| Phase 4: 完善部署 | 2 周 | 2 周 | - |
| **总计** | **8 周** | **10-11 周** | |

Phase 1 的关键任务：
- [ ] 跑通 MathModelAgent 原版 Docker Compose 部署
- [ ] 用 1 道简单题目跑通完整 4-Agent 流程
- [ ] 理解 Agent 间通信协议（A2A schema）
- [ ] 理解 Redis 状态管理
- [ ] 写关键路径的集成测试
- [ ] 改造 CoordinatorAgent → ParserAgent
- [ ] 将 EDA 能力集成到 ModelerAgent

---

## 10. "决策文档"方案建议简化

**现状**: 6 个 Stage 都生成独立的 `0X_阶段名.md` 决策文档

**问题**: 每个 Agent 要同时输出"实际产物"和"决策说明"，增加 prompt 复杂度和 token 消耗。且两个输出之间可能存在不一致。

**建议**:
- 只在最关键的 **Stage 3（建模决策）** 生成独立的 `03_建模决策文档.md`
- 其他阶段的决策说明**嵌入在已有产物中**：
  - Stage 1: 题目解析理由 → 写在 `task.json` 的 `reasoning` 字段
  - Stage 2: EDA 洞察 → 写在 `eda_report.md` 的"分析结论"章节
  - Stage 4: 代码修复记录 → 写在 `notebook.ipynb` 的 markdown cell 中
  - Stage 5: 验证方法 → 写在 `validation_report.md` 中
  - Stage 6: 去 AI 味记录 → 写在论文末尾的"写作说明"附录

---

## 补充建议

### 模型选择策略

文档建议全部使用 DeepSeek V4 Pro，这没问题。但不同阶段对模型能力的需求不同：

| 阶段 | 关键能力 | 模型建议 |
|------|---------|---------|
| Stage 1 题目解析 | 中文理解、结构化输出 | DeepSeek V4 Pro |
| Stage 2 EDA | 数据分析推理 | DeepSeek V4 Pro |
| Stage 3 建模方案 | **最强推理能力** | DeepSeek V4 Pro（最核心环节） |
| Stage 4 代码执行 | 代码生成、错误修复 | DeepSeek V4 Pro 或 DeepSeek-Coder |
| Stage 5 结果验证 | 数值判断、逻辑检查 | 可降级用 DeepSeek-V3 |
| Stage 6 论文生成 | 中文写作、格式控制 | DeepSeek V4 Pro |
| Reviewer（新增） | 质量检查 | 便宜模型即可（如 DeepSeek-V3） |

### 可视化标准

math-modeling-skill 有详细的 SCI/Nature 可视化配置（`编程手说明.md` 第 400-500 行），包括：
```python
plt.rcParams['font.sans-serif'] = ['SimHei', 'DejaVu Sans', 'Arial Unicode MS']
plt.rcParams['axes.linewidth'] = 1.5
plt.rcParams['lines.linewidth'] = 2
# dpi=300, 最多 2 个子图, 禁止饼图/3D图/图内标题
```

建议直接将这些配置集成到 CoderAgent 的 Prompt 中（MathModelAgent 的 `CODER_PROMPT` 已有可视化规范，可以对比补充）。

---

## 总结：优先执行清单

| 优先级 | 事项 | 类型 |
|:------:|------|------|
| 🔴 P0 | Agent 数量从 6 缩减到 5（+1 Reviewer） | 架构调整 |
| 🔴 P0 | 保留 Modeler 后的 HIL checkpoint | 功能保留 |
| 🔴 P0 | 61 算法库接入 RAG 知识库 | 能力增强 |
| 🟡 P1 | 调整实施时间线（Phase 1 延长到 3-4 周） | 计划调整 |
| 🟡 P1 | 简化决策文档方案（只保留 Stage 3 独立文档） | 简化设计 |
| 🟡 P1 | 明确 EDA 的代码执行复用 CoderAgent 基础设施 | 架构明确 |
| 🟢 P2 | 增加 Reviewer Agent 做论文质量自评 | 功能新增 |
| 🟢 P2 | 长上下文问题具体方案 | 技术方案 |
| 🟢 P2 | 评估 docx-js 替代 python-docx | 技术选型 |
| 🟢 P2 | 子问题依赖先顺序执行，不做 DAG | 简化实施 |
